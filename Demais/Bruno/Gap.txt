var
  GAPalta,GAPbaixa           : Booleano;
  valorGAPalta,valorGAPbaixa : Real;
inicio
  // Condição
  GAPalta := abertura > fechamento[1];
  GAPbaixa := abertura < FECHAMENTO[1];
  GAPalta := abertura < fechamento[1];
  GAPbaixa := abertura > FECHAMENTO[1];
  // Valores
  valorGAPalta := abertura + fechamento[1];
  valorGAPbaixa := abertura - fechamento[1];
   valorGAPalta := abertura - fechamento[1];
  valorGAPbaixa := abertura + fechamento[1];
  // Teste Condicional
  Se (GAPbaixa) e (valorGAPbaixa > 05) entao
    inicio
      PaintBar(clblue);
      Plot(1);
    fim
    Senao Se (GAPalta) e (valorGAPalta > 05) entao
    inicio
      PaintBar(clblue);
      Plot(1);
    fim
  Senao Se (GAPalta) e (valorGAPalta < 05) entao
    inicio
      PaintBar(clblue);
      Plot(1);
    fim
    Senao Se (GApbaixa) e (valorGAPbaixa < 05) entao
    inicio
      PaintBar(clblue);
      Plot(1);
    fim
  Senao 
    Plot(0);
end;